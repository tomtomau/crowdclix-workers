{
  "name": "crowdclix_workers",
  "version": "0.0.0",
  "devDependencies": {
    "wrangler": "^3.23.0"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "resetdb": "wrangler d1 execute crowdclix-data --local --file=./schema.sql"
  }
}
